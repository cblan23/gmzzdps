# 叨叨诡秘 Dps-Logs 采集链路风险改造方案

## 1. 目标与硬约束

本方案的目标是逐步降低主动 RPC 和游戏进程 Inline Hook 带来的技术风险，同时保持当前版本的统计准确性、显示结果和历史记录不变。

以下条件是切换数据源的硬门槛，不接受“基本一致”或按比例误差：

- 每场战斗的玩家集合、玩家身份映射和排序一致。
- 每名玩家总伤害为整数精确相等。
- DPS、战斗起止时间和最终显示值一致。
- 技能伤害、命中次数、暴击次数和目标归属一致。
- Boss 实体、模板、名称、等级、血量、阶段和战斗边界一致。
- 历史记录内容与当前用户勾选的字段一致。
- 小怪、木桩、组队成员进出、团灭、重开和跨阶段 Boss 的行为一致。

任何一项不一致，新链路只能继续作为影子链路运行，不能替换当前生产链路。

## 2. 当前链路判断

当前程序并不是自行破解网络加密。它在游戏完成解密、解压和 RPC 解码后，通过 Hook 复制已解码消息。

现有链路包含三类数据源：

1. `NetworkMessageHook`：读取解码后的 RPC，包括逐次伤害、血量、队伍和场景消息。
2. `DamageHook`：补充原生伤害、Boss 类型、实体名称等信息。
3. `TeamStatsRequestHook`：Hook `call_server`，约每秒额外发起一次 `ReqCommonCombatStatisticsByTeam`，得到团队累计伤害快照。

第三类数据目前不是可随手删除的旁路。`RetCommonCombatStatisticsByTeam` 会生成 `team_stat`，而 `CombatModel.ingest_team_stat()` 将其作为权威累计快照，用于：

- 建立开怪前的零值/累计值基线；
- 校准队友总伤害和遗漏事件；
- 识别成员身份和队伍边界；
- 防止快照前的逐次伤害重复计入；
- 在 Boss 结束后刷新最终历史记录。

因此，直接删除 `TeamStatsRequestHook` 很可能改变队友总伤害和最终 DPS，不符合本方案的硬约束。

## 3. 总体架构

改造期间保留一条生产链路和一条影子链路：

```text
同一批采集事件
    |
    +-- Legacy Reducer（当前逻辑）----> 玩家实际看到的结果
    |
    +-- Candidate Reducer（候选逻辑）-> 仅生成匿名差异，不参与 UI/历史记录
                                      |
                                      +-> 精确一致门禁
```

生产链路在候选链路通过全部门禁前保持不变。影子链路不得反向修改生产状态，也不得影响消息确认、Hook 时序或 UI 刷新。

## 4. 分阶段实施

### 阶段 A：冻结结果契约

先把“当前正确结果”固定成可自动比较的规范，而不是边改边凭肉眼判断。

- 为每场战斗生成规范化结果：玩家、总伤害、DPS、技能、暴击、目标、Boss、时长和结束原因。
- 排除只与运行环境有关的字段，例如本地路径、进程号和诊断时间。
- 对规范化 JSON 计算哈希，作为逐场结果指纹。
- 扩充回放样本：单人、四人队、木桩、小怪密集、晚启动、成员中途加入/退出、团灭、Boss 转阶段和两只同名 Boss。
- 将当前所有单元测试和回放结果作为基线；已有 `replay_combat_log.py` 可继续承担离线回放入口。

交付门禁：现有测试全部通过，基线样本可重复回放且结果指纹稳定。

### 阶段 B：数据来源标记与影子计算

不改现有统计器，只给输入事件增加内部来源标记：

- `network_damage`：自然收到的逐次伤害。
- `native_damage`：原生伤害 Hook。
- `team_dirty`：客户端自然收到的脏团队快照。
- `team_full_injected`：由工具额外请求产生的完整团队快照。
- `team_full_natural`：游戏自身流程产生的完整团队快照。

需要给主动请求记录精确请求时间/序号，并把对应响应标为 `team_full_injected`。该标记只用于影子比较，不改变当前 Reducer 的输入。

候选 Reducer 过滤 `team_full_injected`，其余输入与生产链路完全相同。每场战斗结束后比较两份规范化结果，只保存匿名差异摘要，不保存卡号、玩家名或原始封包。

交付门禁：影子链路不会改变主线程延迟、网络消息确认或现有结果。

### 阶段 C：补齐被动链路缺口

根据差异分类补齐，禁止用缩放、估算或容差掩盖差异。

优先检查以下已知缺口：

- 开怪前没有完整零值快照，首个累计值无法确定基线。
- 程序在战斗中途启动，无法声明启动前伤害。
- 脏快照省略未变化成员或零值字段。
- 队友逐次伤害消息缺失，但累计快照包含最终总伤害。
- Boss 死亡后最后一次累计快照晚到。
- 成员 Token、实体 ID 和角色资料绑定时序不同。

允许的补齐方式按优先级排序：

1. 使用客户端本来就会收到的自然 RPC 和场景事件。
2. 使用当前已捕获的逐次伤害、血量和实体事件重建确定性状态。
3. 使用只读、稳定且可验证的客户端状态，但必须证明不会漏掉瞬时事件。
4. 请求游戏方提供官方日志、插件接口或统计 API。

不允许使用屏幕 OCR、按比例补伤害、猜测缺失玩家或把队伍总伤害摊到技能，因为这些方式不能维持当前精度。

交付门禁：离线基线逐字段 100% 一致；真实影子样本覆盖全部场景且连续稳定，不存在未解释差异。

### 阶段 D：停止主动团队 RPC

只有阶段 C 通过后，才在内部测试版停止创建 `TeamStatsRequestHook`。

- 先仅对内部白名单启用，保留快速回滚开关。
- 继续记录候选结果指纹和异常原因。
- 验证服务器侧不再出现工具额外生成的 `ReqCommonCombatStatisticsByTeam`。
- 验证关闭程序、游戏退出、断线重连和崩溃恢复路径。

生产切换门禁：结果精确一致、无新增卡顿/崩溃、无额外主动团队 RPC。未达到门禁时继续使用当前生产链路，不向玩家发布候选实现。

### 阶段 E：逐个缩减 Inline Hook

Inline Hook 必须逐个移除并分别验证，不能一次性重写所有采集代码。

建议顺序：

1. 在影子链路中停用 `DamageHook` 的伤害分支，验证 `OnMsgDamageSyncV2` 是否覆盖所有玩家和技能。
2. 分别替换名称、Boss 类型和 Boss 初始化分支，优先使用自然实体消息和静态模板数据。
3. 每移除一个分支，都执行同一套逐字段结果门禁和长时间真实影子验证。
4. 最后才评估 `NetworkMessageHook`。

最后一步存在明确技术边界：如果游戏没有官方日志、插件 API 或其他授权数据源，仅靠当前外部进程无法同时做到“完全移除解码后消息 Hook”和“保证逐次伤害零丢失、结果完全一致”。网络密文抓包、轮询内存或 OCR 都不能诚实地提供同等级保证。到达该边界时应暂停切换，优先与游戏方确认授权数据接口，而不是以准确性换取表面上的无 Hook。

## 5. 发布与回滚规则

- 改造期间玩家看到的始终是 Legacy 结果，Candidate 只做后台对照。
- 每个阶段单独提交、单独构建、单独回放，避免数据源和 UI 同时变化。
- 发布前提供差异报告：样本数、场景覆盖、完全一致场次、不一致场次及原因。
- 任一精确门禁失败，自动保持或回滚到上一条已验证链路。
- 不把主动 RPC 降频当作最终方案。降频只能降低次数，仍然是主动调用，而且可能改变最终快照时机。
- 不在未验证时把 Team 快照改成可选设置，因为不同用户会得到不同统计口径。

## 6. 建议的首个改造迭代

首个迭代只做可观测性和影子验证，不改变玩家结果：

1. 定义并测试规范化战斗结果与结果指纹。
2. 为完整团队快照标记“自然/工具请求”来源。
3. 建立过滤工具请求响应的 Candidate Reducer。
4. 在历史记录落盘前执行 Legacy/Candidate 精确比较。
5. 输出匿名差异摘要和场景标签。
6. 使用现有日志回放和内部真实战斗建立第一版差异矩阵。

完成这一迭代后，才能基于数据回答“主动团队 RPC 是否可以无损删除”。在此之前直接删除属于不可验证的准确性回退。

## 7. 本次不做的改动

当前“问题检测工具报告上传一直等待”的修复只涉及检测报告生成兼容性、异常兜底和检测进程资源回收，不修改 DPS 解析、伤害计算、Boss 判定、团队快照逻辑或 UI 结果。
